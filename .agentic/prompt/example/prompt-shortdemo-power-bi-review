Using the Power BI Modeling MCP Server, connect to the model currently open in Power BI Desktop (ListConnections → ListLocalInstances → connect). Inspect the model (Get + list tables/relationships/measures) and then give me 10 improvements prioritised by impact, complexity, and disruption risk. For each: exact object to change, why, safe steps, validation, rollback. Group into quick wins / medium / strategic.
